import{NavLink,Outlet,useNavigate}from'react-router-dom';import{Activity,Boxes,ChevronDown,Code2,Globe2,LogOut,Settings,Webhook}from'lucide-react';import{useAuth}from'../contexts/AuthContext';
const links=[['/dashboard','Overview',Activity],['/endpoints','Endpoints',Webhook],['/requests','Requests',Code2],['/environments','Environments',Globe2],['/settings','Settings',Settings]] as const;
export function Layout(){const{user,logout}=useAuth();const nav=useNavigate();return <div className="shell"><aside><div className="brand"><span><Boxes/></span>HookLab</div><div className="workspace"><small>WORKSPACE</small><button><i>H</i><span>HookLab workspace</span><ChevronDown/></button></div><nav>{links.map(([to,label,Icon])=><NavLink key={to} to={to}><Icon/>{label}</NavLink>)}</nav><div className="sidebar-foot"><div className="user"><i>{user?.name?.[0]}</i><span><strong>{user?.name}</strong><small>{user?.email}</small></span><button aria-label="Log out" onClick={()=>{logout();nav('/login')}}><LogOut/></button></div></div></aside><main><Outlet/></main></div>}
