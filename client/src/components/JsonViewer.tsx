import{useState}from'react';import{Check,Copy}from'lucide-react';export function JsonViewer({value}:{value:unknown}){const[copied,setCopied]=useState(false);const json=JSON.stringify(value??{},null,2);return <div className="code-box"><button className="copy" onClick={()=>{navigator.clipboard.writeText(json);setCopied(true);setTimeout(()=>setCopied(false),1200)}}>{copied?<Check/>:<Copy/>}{copied?'Copied':'Copy JSON'}</button><pre>{json}</pre></div>}
