import{useState,type FormEvent}from'react';import{Link,useNavigate}from'react-router-dom';import{Boxes,Eye,EyeOff,LoaderCircle}from'lucide-react';import{useAuth}from'../contexts/AuthContext';
export function AuthPage({mode}:{mode:'login'|'register'}){const auth=useAuth(),nav=useNavigate();const[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[show,setShow]=useState(false),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{if(mode==='login')await auth.login(email,password);else await auth.register(name,email,password);nav('/dashboard')}catch(e){setError((e as {response?:{data?:{error?:{message?:string}}}}).response?.data?.error?.message||'Something went wrong')}finally{setBusy(false)}}return <div className="auth-page"><section className="auth-art"><div className="brand"><span><Boxes/></span>HookLab</div><div className="art-copy"><BadgeLive/><h1>Ship webhooks.<br/><em>Without the guesswork.</em></h1><p>Capture, inspect, and replay any webhook in real time. Built for developers who value clarity.</p><div className="sample-code"><div><b></b><b></b><b></b><span>Live request</span></div><pre><i>POST</i> /hooks/stripe-payments<br/><span>{'{'} "type": "payment.completed",<br/>  "amount": <strong>129.90</strong> {'}'}</span></pre></div></div><footer>Built for developers, by developers.</footer></section><section className="auth-form"><form onSubmit={submit}><div className="mobile-logo"><Boxes/>HookLab</div><small>{mode==='login'?'WELCOME BACK':'START BUILDING'}</small><h2>{mode==='login'?'Sign in to HookLab':'Create your account'}</h2><p>{mode==='login'?'Continue debugging webhooks in seconds.':'Your first workspace is created automatically.'}</p>{mode==='register'&&<label>Name<input autoFocus required minLength={2} value={name} onChange={e=>setName(e.target.value)} placeholder="Emanuel Silva"/></label>}<label>Email address<input autoFocus={mode==='login'} required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com"/></label><label>Password<div className="password"><input required minLength={8} type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label>{error&&<div className="form-error">{error}</div>}<button className="submit" disabled={busy}>{busy?<LoaderCircle className="spin"/>:mode==='login'?'Sign in':'Create account'}</button><div className="auth-switch">{mode==='login'?"Don't have an account? ":'Already have an account? '}<Link to={mode==='login'?'/register':'/login'}>{mode==='login'?'Create one':'Sign in'}</Link></div>{mode==='login'&&<div className="demo">Demo: emanuel@example.com / password123</div>}</form></section></div>}
function BadgeLive(){return <div className="live"><i/>Webhook infrastructure for modern teams</div>}
